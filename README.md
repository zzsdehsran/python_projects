# Get IPs Tool

这是一个用于处理网络设备 IP 地址的 Python 工具。

## 功能特点
- 自动提取/生成特定格式的 IP 地址列表。
- 适用于网络自动化任务（如 FortiGate/Huawei 配置）。

## 环境准备
- Python 3.13
- 依赖库：`pip install -r requirements.txt`（如果有）

## 使用方法
1. 确保你已经在虚拟环境中。
2. 运行脚本：
   ```bash
   python get_ips.py